apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: myapp
  labels:
    app: nginx
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
