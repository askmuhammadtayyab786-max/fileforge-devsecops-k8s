
apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
  namespace: myapp
  labels:
    app: nginx
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
